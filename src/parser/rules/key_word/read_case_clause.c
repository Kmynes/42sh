#include <parser/rules/rules.h>

bool read_case_clause(struct parser *p)
{
    int tmp = p->cursor;

    if (true) {
        // Code the ast here
        return true;
    }

    p->cursor = tmp;

    return false;
}