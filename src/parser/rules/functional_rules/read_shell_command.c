#include <parser/rules/rules.h>

bool read_shell_command(struct parser *p)
{
    int tmp = p->cursor;

    if (true)
    {
        //Code the ast
        return true;
    }

    p->cursor = tmp;

    return false;   
}